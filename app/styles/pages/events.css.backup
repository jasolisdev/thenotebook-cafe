/* ============================================================
   EVENTS PAGE
============================================================ */

/* ============================================================
   HERO SECTION
============================================================ */
.events-hero {
  padding-top: calc(var(--announcement-banner-height, 42px) + clamp(96px, 12vh, 120px));
  padding-bottom: clamp(48px, 10vh, 72px);
  transition: padding-top 0.3s ease;
}

@media (min-width: 375px) {
  .events-hero {
    padding-top: calc(var(--announcement-banner-height, 46px) + clamp(104px, 13vh, 128px));
    padding-bottom: clamp(56px, 11vh, 84px);
  }
}

@media (min-width: 640px) {
  .events-hero {
    padding-top: calc(var(--announcement-banner-height, 48px) + clamp(112px, 14vh, 136px));
    padding-bottom: clamp(64px, 12vh, 96px);
  }
}

@media (min-width: 1024px) {
  .events-hero {
    padding-top: calc(var(--announcement-banner-height, 48px) + clamp(120px, 15vh, 144px));
    padding-bottom: clamp(72px, 13vh, 104px);
  }
}

.events-hero-content {
  position: relative;
}

.events-badge {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 10px 20px;
  background: linear-gradient(135deg, rgba(164, 141, 120, 0.12), rgba(164, 141, 120, 0.06));
  border: 1px solid rgba(164, 141, 120, 0.25);
  border-radius: 9999px;
  margin-bottom: 24px;
  font-size: 11px;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  color: rgba(164, 141, 120, 0.9);
  box-shadow: 0 4px 12px rgba(164, 141, 120, 0.12);
}

@media (min-width: 375px) {
  .events-badge {
    padding: 10px 20px;
    margin-bottom: 26px;
    font-size: 12px;
  }
}

@media (min-width: 640px) {
  .events-badge {
    padding: 12px 24px;
    margin-bottom: 28px;
    font-size: 12px;
  }
}

.events-badge-icon {
  color: rgba(164, 141, 120, 0.9);
  animation: pulse 3s ease-in-out infinite;
}

.events-title {
  font-family: var(--font-display);
  font-size: 32px;
  font-weight: 700;
  line-height: 1.15;
  letter-spacing: -0.01em;
  color: #f6ebdf;
  margin-bottom: 18px;
  text-shadow: 0 6px 24px rgba(0, 0, 0, 0.35);
}

@media (min-width: 375px) {
  .events-title {
    font-size: 38px;
    margin-bottom: 22px;
  }
}

@media (min-width: 640px) {
  .events-title {
    font-size: 46px;
    margin-bottom: 24px;
  }
}

@media (min-width: 768px) {
  .events-title {
    font-size: 52px;
    margin-bottom: 28px;
  }
}

@media (min-width: 1024px) {
  .events-title {
    font-size: 58px;
  }
}

.events-tagline {
  font-family: var(--font-sans);
  font-size: 14px;
  font-weight: 400;
  line-height: 1.7;
  letter-spacing: 0.01em;
  color: rgba(246, 235, 223, 0.78);
  max-width: 720px;
  margin: 0 auto 36px;
}

@media (min-width: 375px) {
  .events-tagline {
    font-size: 15px;
    margin-bottom: 40px;
  }
}

@media (min-width: 640px) {
  .events-tagline {
    font-size: 16px;
    margin-bottom: 44px;
  }
}

@media (min-width: 768px) {
  .events-tagline {
    font-size: 17px;
    margin-bottom: 48px;
  }
}

/* Event Details Grid */
.events-details-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
  max-width: 600px;
  margin: 0 auto;
}

@media (min-width: 640px) {
  .events-details-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    max-width: 800px;
  }
}

.events-detail-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 16px;
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(164, 141, 120, 0.2);
  border-radius: 14px;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.25), inset 0 1px 0 rgba(255, 255, 255, 0.02);
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.3s cubic-bezier(0.4, 0, 0.2, 1), border-color 0.3s cubic-bezier(0.4, 0, 0.2, 1), background 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
}

@media (min-width: 640px) {
  .events-detail-item {
    flex-direction: column;
    text-align: center;
    padding: 20px;
  }
}

.events-detail-item::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(circle at top left, rgba(164, 141, 120, 0.06), transparent 60%);
  opacity: 0;
  transition: opacity 0.3s;
}

.events-detail-item:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 32px rgba(0, 0, 0, 0.35), 0 0 24px rgba(164, 141, 120, 0.12), inset 0 1px 0 rgba(255, 255, 255, 0.05);
  border-color: rgba(164, 141, 120, 0.3);
  background: rgba(255, 255, 255, 0.05);
}

.events-detail-item:hover::before {
  opacity: 1;
}

.events-detail-icon {
  width: 24px;
  height: 24px;
  color: rgba(164, 141, 120, 0.8);
  flex-shrink: 0;
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), color 0.3s;
}

.events-detail-item:hover .events-detail-icon {
  transform: scale(1.15) rotate(-5deg);
  color: rgba(164, 141, 120, 1);
}

@media (min-width: 640px) {
  .events-detail-icon {
    width: 28px;
    height: 28px;
    margin-bottom: 8px;
  }
}

.events-detail-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: rgba(240, 215, 192, 0.78);
  margin-bottom: 4px;
}

.events-detail-value {
  font-size: 14px;
  font-weight: 600;
  color: #f6ebdf;
}

@media (min-width: 375px) {
  .events-detail-value {
    font-size: 15px;
  }
}

/* ============================================================
   CREAM SECTION STYLES
============================================================ */
.events-dj-image {
  width: 100%;
  max-width: 900px;
  height: auto;
  border-radius: 16px;
  margin: 0 auto;
  display: block;
  box-shadow: 0 8px 32px rgba(42, 31, 22, 0.15);
}

@media (min-width: 640px) {
  .events-dj-image {
    border-radius: 20px;
  }
}

/* Section Label with Dividers */
.events-section-label {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 24px;
  margin-bottom: 32px;
}

@media (min-width: 640px) {
  .events-section-label {
    margin-bottom: 40px;
  }
}

.events-divider-line {
  flex: 1;
  max-width: 120px;
  height: 1px;
  background: linear-gradient(to right, transparent, rgba(201, 154, 88, 0.3), transparent);
}

.events-label-text {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 2px;
  color: var(--warm-roast);
  white-space: nowrap;
}

/* DJ Card */
.events-dj-card {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.7) 0%, rgba(255, 255, 255, 0.5) 100%);
  border: 1.5px solid rgba(201, 154, 88, 0.25);
  border-radius: 16px;
  padding: 24px;
  box-shadow: 0 4px 12px rgba(42, 31, 22, 0.08), inset 0 1px 0 rgba(255, 255, 255, 0.6);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

@media (min-width: 640px) {
  .events-dj-card {
    padding: 32px;
    border-radius: 20px;
  }
}

.events-dj-card:hover {
  transform: translateY(-4px);
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%);
  border-color: rgba(201, 154, 88, 0.4);
  box-shadow: 0 8px 24px rgba(42, 31, 22, 0.12), inset 0 1px 0 rgba(255, 255, 255, 0.8);
}

.events-dj-card-header {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 16px;
}

.events-dj-icon {
  width: 48px;
  height: 48px;
  color: rgba(201, 154, 88, 0.9);
  padding: 10px;
  background: rgba(201, 154, 88, 0.1);
  border: 2px solid rgba(201, 154, 88, 0.2);
  border-radius: 12px;
  flex-shrink: 0;
}

.events-dj-name {
  font-family: var(--font-display);
  font-size: 24px;
  font-weight: 700;
  letter-spacing: 0.5px;
  color: #2a1f16;
  margin: 0 0 4px 0;
}

@media (min-width: 640px) {
  .events-dj-name {
    font-size: 28px;
  }
}

.events-dj-genre {
  font-size: 13px;
  font-weight: 500;
  color: rgba(201, 154, 88, 0.9);
  margin: 0;
}

@media (min-width: 640px) {
  .events-dj-genre {
    font-size: 14px;
  }
}

.events-dj-bio {
  font-size: 14.5px;
  line-height: 1.7;
  color: #2a1f16;
  margin: 0;
}

@media (min-width: 640px) {
  .events-dj-bio {
    font-size: 15.5px;
  }
}

/* Highlights Grid */
.events-highlights-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
}

@media (min-width: 640px) {
  .events-highlights-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }
}

.events-highlight-card {
  display: flex;
  align-items: flex-start;
  gap: 16px;
  padding: 20px;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.7) 0%, rgba(255, 255, 255, 0.5) 100%);
  border: 1.5px solid rgba(201, 154, 88, 0.25);
  border-radius: 12px;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 4px 12px rgba(42, 31, 22, 0.08), inset 0 1px 0 rgba(255, 255, 255, 0.6);
}

@media (min-width: 640px) {
  .events-highlight-card {
    padding: 24px;
    border-radius: 14px;
  }
}

.events-highlight-card:hover {
  transform: translateY(-4px);
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%);
  border-color: rgba(201, 154, 88, 0.4);
  box-shadow: 0 8px 24px rgba(42, 31, 22, 0.12), inset 0 1px 0 rgba(255, 255, 255, 0.8);
}

.events-highlight-icon-wrapper {
  width: 48px;
  height: 48px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(201, 154, 88, 0.1);
  border: 2px solid rgba(201, 154, 88, 0.2);
  border-radius: 50%;
  color: rgba(201, 154, 88, 0.9);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.events-highlight-card:hover .events-highlight-icon-wrapper {
  background: rgba(201, 154, 88, 0.15);
  border-color: rgba(201, 154, 88, 0.4);
  transform: scale(1.05);
  color: rgba(201, 154, 88, 1);
}

.events-highlight-title {
  font-family: var(--font-sans);
  font-size: 15px;
  font-weight: 600;
  color: #2a1f16;
  margin: 0 0 6px 0;
}

@media (min-width: 640px) {
  .events-highlight-title {
    font-size: 16px;
  }
}

.events-highlight-text {
  font-size: 13.5px;
  line-height: 1.6;
  color: rgba(42, 31, 22, 0.8);
  margin: 0;
}

@media (min-width: 640px) {
  .events-highlight-text {
    font-size: 14px;
  }
}

/* ============================================================
   VIP CARD
============================================================ */
.events-vip-card {
  background: linear-gradient(135deg,
    rgba(201, 154, 88, 0.12) 0%,
    rgba(201, 154, 88, 0.08) 100%);
  border: 2px solid rgba(201, 154, 88, 0.3);
  border-radius: 20px;
  padding: 32px 24px;
  text-align: center;
  position: relative;
  overflow: hidden;
  box-shadow:
    0 8px 32px rgba(201, 154, 88, 0.15),
    inset 0 1px 0 rgba(255, 255, 255, 0.5);
}

@media (min-width: 640px) {
  .events-vip-card {
    padding: 40px 48px;
    border-radius: 24px;
  }
}

.events-vip-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg,
    transparent,
    rgba(201, 154, 88, 0.6),
    transparent);
}

.events-vip-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  background: rgba(201, 154, 88, 1);
  color: #0f0c0a;
  border-radius: 50px;
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1.2px;
  margin-bottom: 20px;
}

@media (min-width: 640px) {
  .events-vip-badge {
    padding: 10px 20px;
    font-size: 12px;
    margin-bottom: 24px;
  }
}

.events-vip-title {
  font-family: var(--font-display);
  font-size: 26px;
  font-weight: 700;
  letter-spacing: -0.01em;
  color: #2a1f16;
  margin: 0 0 16px 0;
}

@media (min-width: 640px) {
  .events-vip-title {
    font-size: 32px;
    margin-bottom: 20px;
  }
}

.events-vip-description {
  font-size: 14.5px;
  line-height: 1.7;
  color: #2a1f16;
  margin: 0 0 24px 0;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

@media (min-width: 640px) {
  .events-vip-description {
    font-size: 15.5px;
    margin-bottom: 28px;
  }
}

.events-vip-perks {
  display: grid;
  grid-template-columns: 1fr;
  gap: 10px;
  margin-bottom: 32px;
  max-width: 500px;
  margin-left: auto;
  margin-right: auto;
}

@media (min-width: 640px) {
  .events-vip-perks {
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
    margin-bottom: 36px;
  }
}

.events-vip-perk {
  font-size: 13.5px;
  font-weight: 500;
  color: rgba(42, 31, 22, 0.9);
  padding: 10px;
  background: rgba(255, 255, 255, 0.5);
  border-radius: 8px;
  text-align: left;
}

@media (min-width: 640px) {
  .events-vip-perk {
    font-size: 14px;
    padding: 12px;
  }
}

.events-vip-form {
  max-width: 500px;
  margin: 0 auto 16px;
}

.events-vip-note {
  font-size: 11px;
  color: rgba(42, 31, 22, 0.6);
  margin: 0;
  font-style: italic;
}

@media (min-width: 640px) {
  .events-vip-note {
    font-size: 12px;
  }
}

/* ============================================================
   GALLERY
============================================================ */
.events-gallery {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
}

@media (min-width: 640px) {
  .events-gallery {
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }
}

.events-gallery-item {
  border-radius: 12px;
  overflow: hidden;
  aspect-ratio: 4/3;
  box-shadow: 0 4px 12px rgba(42, 31, 22, 0.1);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

@media (min-width: 640px) {
  .events-gallery-item {
    border-radius: 16px;
  }
}

.events-gallery-item:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 0 8px 24px rgba(42, 31, 22, 0.15);
}

.events-gallery-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

.events-gallery-item:hover .events-gallery-image {
  transform: scale(1.05);
}
